# Web de pruebas — Auth (signin/signup → dashboard) — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Una web Vue en `margin/web/` (monorepo) con registro, login y un dashboard mínimo, consumiendo el backend real, con el estilo del design-system y CORS habilitado en el backend.

**Architecture:** Vue 3 + Vite + Vue Router. Capas: `src/api/` (cliente fetch + sesión en localStorage), `src/router/` (rutas + guard), `src/pages/` (Login, Register, Dashboard), `src/style.css` (tokens del design-system). El backend suma `CORSMiddleware`. Verificación por `npm run build` + prueba manual en el navegador (sin tests automatizados de frontend por ahora).

**Tech Stack:** Vue 3.5, Vite 6, Vue Router 4, Node 25 / npm 11. Backend FastAPI (ya existe).

**Spec:** `docs/superpowers/specs/2026-06-06-web-auth-design.md`. Estructura: sección 5 de `...estructura-y-flujo-de-trabajo-design.md`.

---

## Estructura de archivos

```
margin/
├── backend/app/
│   ├── core/config.py        # + cors_origins   (MODIFICAR)
│   └── main.py               # + CORSMiddleware   (MODIFICAR)
├── backend/tests/test_cors.py  # (NUEVO)
└── web/                       # (NUEVO — todo el proyecto Vue)
    ├── package.json
    ├── vite.config.js
    ├── index.html
    ├── .env.example
    ├── .env                   # (no se commitea)
    ├── CLAUDE.md
    └── src/
        ├── main.js
        ├── App.vue
        ├── style.css
        ├── api/index.js
        ├── router/index.js
        └── pages/
            ├── Login.vue
            ├── Register.vue
            └── Dashboard.vue
```

---

## Task 1: Backend — habilitar CORS (TDD)

**Files:** Modify `backend/app/core/config.py`, `backend/app/main.py`, Create `backend/tests/test_cors.py`

- [ ] **Step 1: Agregar `cors_origins` a `Settings` (`backend/app/core/config.py`)**

Agregar dentro de la clase `Settings`, después de `jwt_expire_days`:

```python
    cors_origins: list[str] = ["http://localhost:5173", "http://127.0.0.1:5173"]
```

- [ ] **Step 2: Test que falla `backend/tests/test_cors.py`**

```python
def test_cors_allows_web_origin(client):
    response = client.options(
        "/auth/login",
        headers={
            "Origin": "http://localhost:5173",
            "Access-Control-Request-Method": "POST",
        },
    )
    assert response.status_code == 200
    assert response.headers["access-control-allow-origin"] == "http://localhost:5173"
```

- [ ] **Step 3: Correr y verificar que falla**

Run: `cd /Users/tachone/proyectos/margin/backend && source .venv/bin/activate && pytest tests/test_cors.py -v`
Expected: FALLA (sin CORS, el preflight no devuelve el header / no es 200).

- [ ] **Step 4: Agregar el middleware en `backend/app/main.py`**

Reemplazar el contenido completo por:

```python
from fastapi import FastAPI
from fastapi.middleware.cors import CORSMiddleware

from app.core.config import settings
from app.core.errors import register_error_handlers
from app.routers import auth, countries, health

app = FastAPI(title=settings.app_name)

app.add_middleware(
    CORSMiddleware,
    allow_origins=settings.cors_origins,
    allow_credentials=True,
    allow_methods=["*"],
    allow_headers=["*"],
)

register_error_handlers(app)
app.include_router(health.router)
app.include_router(countries.router)
app.include_router(auth.router)
```

- [ ] **Step 5: Correr y verificar que pasa + suite completa**

Run: `pytest -q`
Expected: pasan todos (incluido el nuevo test de CORS).

- [ ] **Step 6: Commit**

```bash
cd /Users/tachone/proyectos/margin
git add backend/app/core/config.py backend/app/main.py backend/tests/test_cors.py
git commit -m "feat(backend): habilitar CORS para la web de pruebas"
```

---

## Task 2: Scaffold del proyecto Vue

**Files:** Create `web/package.json`, `web/vite.config.js`, `web/index.html`, `web/.env.example`, `web/.env`, `web/src/main.js`, `web/src/App.vue`

- [ ] **Step 1: `web/package.json`**

```json
{
  "name": "margin-web",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "vue": "^3.5.13",
    "vue-router": "^4.5.0"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^5.2.1",
    "vite": "^6.0.7"
  }
}
```

- [ ] **Step 2: `web/vite.config.js`**

```js
import { defineConfig } from 'vite'
import vue from '@vitejs/plugin-vue'

export default defineConfig({
  plugins: [vue()],
})
```

- [ ] **Step 3: `web/index.html`**

```html
<!doctype html>
<html lang="es">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Margin — pruebas</title>
  </head>
  <body>
    <div id="app"></div>
    <script type="module" src="/src/main.js"></script>
  </body>
</html>
```

- [ ] **Step 4: `web/.env.example` y `web/.env`**

Ambos con el mismo contenido:

```
VITE_API_BASE_URL=http://localhost:8000
```

- [ ] **Step 5: `web/src/main.js`**

```js
import { createApp } from 'vue'
import './style.css'
import App from './App.vue'
import router from './router'

createApp(App).use(router).mount('#app')
```

- [ ] **Step 6: `web/src/App.vue`**

```vue
<script setup></script>

<template>
  <router-view />
</template>
```

- [ ] **Step 7: Instalar dependencias**

Run: `cd /Users/tachone/proyectos/margin/web && npm install`
Expected: crea `node_modules/` y `package-lock.json` sin errores.

- [ ] **Step 8: Verificar que `node_modules` queda ignorado por git**

Run: `cd /Users/tachone/proyectos/margin && git status -s web/ | grep -c node_modules`
Expected: `0` (node_modules está en `.gitignore`).

> Nota: `App.vue` importa `./router` y `./style.css`, que se crean en tasks siguientes. El build recién se prueba en la Task 6, cuando todas las piezas existen.

- [ ] **Step 9: Commit**

```bash
cd /Users/tachone/proyectos/margin
git add web/package.json web/package-lock.json web/vite.config.js web/index.html web/.env.example web/src/main.js web/src/App.vue
git commit -m "chore(web): scaffold de Vue + Vite en margin/web"
```

---

## Task 3: Estilo (tokens del design-system)

**Files:** Create `web/src/style.css`

- [ ] **Step 1: `web/src/style.css`**

```css
@import url('https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap');

:root {
  --bg: #F5F5F5;
  --card: #FFFFFF;
  --accent: #4F46E5;
  --accent-hover: #4338CA;
  --text: #111827;
  --text-secondary: #6B7280;
  --error: #EF4444;
  --positive: #22C55E;
  --border: #E5E7EB;
  --radius: 12px;
  --max-width: 390px;
}

* { box-sizing: border-box; margin: 0; padding: 0; }

body {
  background: var(--bg);
  font-family: 'Inter', system-ui, sans-serif;
  color: var(--text);
  -webkit-font-smoothing: antialiased;
}

#app { min-height: 100svh; display: flex; flex-direction: column; }

.screen { min-height: 100svh; display: flex; flex-direction: column; background: var(--bg); }

.content {
  padding: 1rem;
  display: flex;
  flex-direction: column;
  gap: 1rem;
  max-width: var(--max-width);
  width: 100%;
  margin: 0 auto;
}

h1 { font-size: 1.25rem; font-weight: 700; }
h2 { font-size: 1rem; font-weight: 600; }

.field { display: flex; flex-direction: column; gap: 0.375rem; }
label { font-size: 0.85rem; font-weight: 500; color: var(--text); }

input {
  padding: 0.75rem 1rem;
  border: 1px solid var(--border);
  border-radius: 8px;
  font-size: 0.95rem;
  font-family: inherit;
  color: var(--text);
  background: white;
  outline: none;
  transition: border-color 0.15s;
}
input:focus { border-color: var(--accent); }

.primary {
  padding: 0.8rem;
  background: var(--accent);
  color: white;
  border: none;
  border-radius: 8px;
  font-size: 0.95rem;
  font-weight: 600;
  font-family: inherit;
  cursor: pointer;
}
.primary:hover { background: var(--accent-hover); }

.ghost {
  background: none;
  border: 1px solid var(--border);
  border-radius: 8px;
  padding: 0.55rem 0.9rem;
  font-size: 0.9rem;
  font-family: inherit;
  color: var(--text-secondary);
  cursor: pointer;
}
.ghost:hover { color: var(--accent); border-color: var(--accent); }

.muted { color: var(--text-secondary); font-size: 0.9rem; }
.error { color: var(--error); font-size: 0.85rem; }

a { color: var(--accent); }
```

- [ ] **Step 2: Commit**

```bash
cd /Users/tachone/proyectos/margin
git add web/src/style.css
git commit -m "feat(web): estilo con tokens del design-system"
```

---

## Task 4: Cliente del API + sesión (`src/api/`)

**Files:** Create `web/src/api/index.js`

- [ ] **Step 1: `web/src/api/index.js`**

```js
const BASE = import.meta.env.VITE_API_BASE_URL || 'http://localhost:8000'

async function request(method, path, body) {
  const token = localStorage.getItem('token')
  const res = await fetch(`${BASE}${path}`, {
    method,
    headers: {
      'Content-Type': 'application/json',
      ...(token ? { Authorization: `Bearer ${token}` } : {}),
    },
    body: body ? JSON.stringify(body) : undefined,
  })
  if (res.status === 204) return null
  const data = await res.json().catch(() => ({}))
  if (!res.ok) {
    // Envelope de error del backend: {code, message, field?} o {code, message, errors:[]}
    const err = new Error(data.message || 'Error inesperado')
    err.code = data.code
    err.field = data.field
    err.errors = data.errors
    throw err
  }
  return data
}

export const api = {
  register(email, password, displayName) {
    const body = { email, password }
    if (displayName) body.display_name = displayName
    return request('POST', '/auth/register', body)
  },
  login(email, password) {
    return request('POST', '/auth/login', { email, password })
  },
}

export function saveSession({ user, token }) {
  localStorage.setItem('token', token)
  localStorage.setItem('user', JSON.stringify(user))
}

export function getUser() {
  const raw = localStorage.getItem('user')
  return raw ? JSON.parse(raw) : null
}

export function clearSession() {
  localStorage.removeItem('token')
  localStorage.removeItem('user')
}

export function isAuthenticated() {
  return !!localStorage.getItem('token')
}
```

- [ ] **Step 2: Commit**

```bash
cd /Users/tachone/proyectos/margin
git add web/src/api/index.js
git commit -m "feat(web): cliente del API (register/login) y sesión en localStorage"
```

---

## Task 5: Router + guard (`src/router/`)

**Files:** Create `web/src/router/index.js`

- [ ] **Step 1: `web/src/router/index.js`**

```js
import { createRouter, createWebHistory } from 'vue-router'

import { isAuthenticated } from '../api'
import Login from '../pages/Login.vue'
import Register from '../pages/Register.vue'
import Dashboard from '../pages/Dashboard.vue'

const router = createRouter({
  history: createWebHistory(),
  routes: [
    { path: '/', redirect: () => (isAuthenticated() ? '/dashboard' : '/login') },
    { path: '/login', component: Login, meta: { public: true } },
    { path: '/register', component: Register, meta: { public: true } },
    { path: '/dashboard', component: Dashboard },
  ],
})

router.beforeEach((to) => {
  if (!to.meta.public && !isAuthenticated()) return '/login'
  return true
})

export default router
```

- [ ] **Step 2: Commit**

```bash
cd /Users/tachone/proyectos/margin
git add web/src/router/index.js
git commit -m "feat(web): router con guard de autenticación"
```

---

## Task 6: Páginas Login, Register, Dashboard (`src/pages/`)

**Files:** Create `web/src/pages/Login.vue`, `web/src/pages/Register.vue`, `web/src/pages/Dashboard.vue`

- [ ] **Step 1: `web/src/pages/Login.vue`**

```vue
<script setup>
import { ref } from 'vue'
import { useRouter } from 'vue-router'
import { api, saveSession } from '../api'

const router = useRouter()
const email = ref('')
const password = ref('')
const error = ref('')

async function submit() {
  error.value = ''
  try {
    const data = await api.login(email.value, password.value)
    saveSession(data)
    router.push('/dashboard')
  } catch (e) {
    error.value = e.message
  }
}
</script>

<template>
  <div class="screen">
    <div class="content">
      <h1>Ingresar</h1>
      <form class="content" style="padding: 0" @submit.prevent="submit">
        <div class="field">
          <label>Email</label>
          <input v-model="email" type="email" autocomplete="email" />
        </div>
        <div class="field">
          <label>Contraseña</label>
          <input v-model="password" type="password" autocomplete="current-password" />
        </div>
        <p v-if="error" class="error">{{ error }}</p>
        <button class="primary" type="submit">Ingresar</button>
      </form>
      <p class="muted">¿No tenés cuenta? <router-link to="/register">Registrate</router-link></p>
    </div>
  </div>
</template>
```

- [ ] **Step 2: `web/src/pages/Register.vue`**

```vue
<script setup>
import { ref } from 'vue'
import { useRouter } from 'vue-router'
import { api, saveSession } from '../api'

const router = useRouter()
const email = ref('')
const password = ref('')
const displayName = ref('')
const error = ref('')

async function submit() {
  error.value = ''
  try {
    const data = await api.register(email.value, password.value, displayName.value)
    saveSession(data)
    router.push('/dashboard')
  } catch (e) {
    error.value = e.message
  }
}
</script>

<template>
  <div class="screen">
    <div class="content">
      <h1>Crear cuenta</h1>
      <form class="content" style="padding: 0" @submit.prevent="submit">
        <div class="field">
          <label>Email</label>
          <input v-model="email" type="email" autocomplete="email" />
        </div>
        <div class="field">
          <label>Contraseña</label>
          <input v-model="password" type="password" autocomplete="new-password" />
        </div>
        <div class="field">
          <label>Nombre (opcional)</label>
          <input v-model="displayName" type="text" autocomplete="name" />
        </div>
        <p v-if="error" class="error">{{ error }}</p>
        <button class="primary" type="submit">Registrarme</button>
      </form>
      <p class="muted">¿Ya tenés cuenta? <router-link to="/login">Ingresá</router-link></p>
    </div>
  </div>
</template>
```

- [ ] **Step 3: `web/src/pages/Dashboard.vue`**

```vue
<script setup>
import { useRouter } from 'vue-router'
import { getUser, clearSession } from '../api'

const router = useRouter()
const user = getUser()

function logout() {
  clearSession()
  router.push('/login')
}
</script>

<template>
  <div class="screen">
    <div class="content">
      <h1>Dashboard</h1>
      <div class="field">
        <label>ID</label>
        <p class="muted">{{ user?.id }}</p>
      </div>
      <div class="field">
        <label>País</label>
        <p class="muted">{{ user?.country_code }}</p>
      </div>
      <div class="field">
        <label>Nombre</label>
        <p class="muted">{{ user?.display_name || '—' }}</p>
      </div>
      <button class="ghost" @click="logout">Cerrar sesión</button>
    </div>
  </div>
</template>
```

- [ ] **Step 4: Verificar que compila (build)**

Run: `cd /Users/tachone/proyectos/margin/web && npm run build`
Expected: build OK, sin errores (genera `dist/`). Esto confirma que todos los imports y componentes resuelven.

- [ ] **Step 5: Commit**

```bash
cd /Users/tachone/proyectos/margin
git add web/src/pages
git commit -m "feat(web): páginas Login, Register y Dashboard"
```

---

## Task 7: Verificación en vivo + CLAUDE.md

**Files:** Create `web/CLAUDE.md`

- [ ] **Step 1: Verificación manual del flujo (dos procesos)**

En una terminal, el backend:
```bash
cd /Users/tachone/proyectos/margin/backend && source .venv/bin/activate && uvicorn app.main:app --reload
```
En otra, la web:
```bash
cd /Users/tachone/proyectos/margin/web && npm run dev
```
En el navegador (`http://localhost:5173`), verificar:
1. `/register` → crear cuenta (email + password ≥ 8) → llega a `/dashboard` con id, país (UY) y nombre.
2. **Cerrar sesión** → vuelve a `/login`.
3. `/login` con esas credenciales → dashboard.
4. Registrar el mismo email otra vez → muestra "Ese email ya está registrado."
5. Login con password mala → "Credenciales inválidas."
6. Ir a `http://localhost:5173/dashboard` sin sesión (después de logout) → redirige a `/login`.

Si algún paso falla, depurar antes de seguir (revisar consola del navegador y log de uvicorn; el caso típico es CORS si la Task 1 no quedó bien).

- [ ] **Step 2: Limpiar usuarios de prueba del backend**

Run: `psql -d margin -c "delete from auth_identities; delete from users;"`
Expected: borra los usuarios creados en la prueba.

- [ ] **Step 3: `web/CLAUDE.md`**

```markdown
# Web (Margin) — banco de pruebas

Vue 3 + Vite + Vue Router. Cliente de prueba del backend (no es la app móvil real, que es otro repo).
Usa el estilo del design-system (Inter, índigo, mobile-first 390px).

## Comandos
- Dev: `npm run dev`  (http://localhost:5173)
- Build: `npm run build`
- Requiere el backend corriendo en la URL de `VITE_API_BASE_URL` (ver `.env`).

## Estructura (sección 5 del spec de estructura)
- `src/api/` — cliente fetch del backend + sesión en localStorage. URL base desde `VITE_API_BASE_URL`.
- `src/router/` — rutas + guard (las no-públicas exigen token).
- `src/pages/` — una página por pantalla (Login, Register, Dashboard).
- `src/style.css` — tokens del design-system + componentes.

## Convenciones
- URL del backend NUNCA hardcodeada: sale de `import.meta.env.VITE_API_BASE_URL`.
- Errores del backend: el cliente parsea `{code, message, field?}` y la página muestra `message`.
- Token + user en `localStorage` (cliente de prueba en dev; no es producción).
```

- [ ] **Step 4: Commit**

```bash
cd /Users/tachone/proyectos/margin
git add web/CLAUDE.md
git commit -m "docs(web): CLAUDE.md de la web de pruebas"
```

---

## Notas de cierre

- Al terminar: la web levanta, registro/login andan contra el backend real (CORS OK), el dashboard muestra el usuario y el guard protege `/dashboard`.
- **Siguiente posible:** endpoint protegido `GET /me` (y consumirlo desde el dashboard para validar el token server-side); o las próximas páginas de prueba (countries, etc.).

## Self-review (writing-plans)

- **Cobertura del spec:** proyecto Vue en `margin/web/` (T2) ✓; estilo design-system (T3) ✓; `api/` con register/login + URL por env + envelope de error (T4) ✓; router + guard (T5) ✓; páginas Login/Register/Dashboard (T6) ✓; CORS en backend (T1) ✓; verificación manual (T7) ✓; CLAUDE.md (T7) ✓. Estructura `pages/router/api` alineada con la sección 5.
- **Placeholders:** ninguno; cada archivo trae su contenido completo.
- **Consistencia:** `api/index.js` exporta `api`, `saveSession`, `getUser`, `clearSession`, `isAuthenticated`; router y páginas importan exactamente esos nombres desde `../api`. Las páginas viven en `src/pages/` y el router las importa desde `../pages/`. El body de register usa `display_name` (contrato real del backend).
